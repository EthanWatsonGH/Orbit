using UnityEngine;

// Add this marker to UI whose fingers should not become world pointers or
// contribute to camera/other multi-touch gestures. Examples include held
// modifier keys and future UI that changes input modes without editing a level.
public class PointerGestureExclusion : MonoBehaviour
{
}
